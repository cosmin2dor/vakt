// Package api is the inbound adapter: HTTP handlers, the SSE hub, and
// static serving of the built PWA. It depends on internal/engine; the
// engine depends on nothing in here. See SDD.md §2.6.
package api
