version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - govet

formatters:
  enable:
    - gofmt
    - goimports

issues:
  exclude-dirs:
    - web
