version: "2"

run:
  timeout: 5m

linters:
  enable:
    - errcheck
    - staticcheck
    - unused
    - ineffassign
    - govet
  exclusions:
    paths:
      - web

formatters:
  enable:
    - gofmt
    - goimports
